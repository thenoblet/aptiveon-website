/**
 * The Aptiveon brand glyph used as a section marker. Replaces legacy
 * `§ 01.X` numbering — same visual mark used in the wordmark.
 */
export default function SectionMark() {
  return <span className="glyph section-mark" aria-hidden="true" />;
}
